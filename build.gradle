apply plugin: 'java'

sourceSets {
  main {
    java.outputDir = file('bin')
    java {
      srcDirs = ['src']
    }    
  }
}

dependencies {
  compile fileTree(dir: 'jars', include: ['*.jar'])

}

