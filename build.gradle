apply plugin: 'java'
jar.enabled = true

project.buildDir = 'bin'

sourceSets {
  main {
    java.outputDir = file('bin/compiled')
    java {
      srcDirs = ['src']
    }    
  }
}

dependencies {
  compile fileTree(dir: 'jars', include: ['*.jar'])

}

